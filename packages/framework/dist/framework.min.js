(function (global, factory) {
  typeof exports === 'object' && typeof module !== 'undefined' ? module.exports = factory(require('@undercoatcss/backdrop')) :
  typeof define === 'function' && define.amd ? define(['@undercoatcss/backdrop'], factory) :
  (global = global || self, global.undercoat = factory(global.backdrop));
}(this, function (backdrop) { 'use strict';

  backdrop = backdrop && backdrop.hasOwnProperty('default') ? backdrop['default'] : backdrop;

  function undercoat() {
    console.log('undercoat');
  }

  return undercoat;

}));
